# 🌸 Task 1: Iris Dataset Exploratory Data Analysis (EDA)

This task performs an exploratory data analysis (EDA) on the famous **Iris flower dataset** using `pandas`, `seaborn`, and `matplotlib`. The goal is to understand the structure, distribution, and relationships within the dataset.

---

## 📁 Dataset Used

We use the built-in `iris` dataset available via the `seaborn` library. It contains 150 rows and 5 columns (4 numeric features + species label).

| Feature         | Description              |
|----------------|--------------------------|
| sepal_length   | Sepal length in cm       |
| sepal_width    | Sepal width in cm        |
| petal_length   | Petal length in cm       |
| petal_width    | Petal width in cm        |
| species        | Iris species (setosa, versicolor, virginica) |

---

## 📊 What This Task Covers

1. Loading the Iris dataset.
2. Displaying basic info (structure, missing values, statistics).
3. Visualizations:
   - Scatter plot (Sepal length vs. width, colored by species)
   - Histograms of all features
   - Box plots for feature distribution and outliers

---

## 🧪 Libraries Used

- `pandas`
- `seaborn`
- `matplotlib`

Install them if needed using:

```bash
pip install pandas seaborn matplotlib
